{
  "routes": [
    {
      "method": "GET",
      "path": "/titles",
      "handler": "Title.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/titles/count",
      "handler": "Title.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/titles/:_id",
      "handler": "Title.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/titles",
      "handler": "Title.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/titles/:_id",
      "handler": "Title.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/titles/:_id",
      "handler": "Title.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
